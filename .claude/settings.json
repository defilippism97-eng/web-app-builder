{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ \"$CLAUDE_CODE_REMOTE\" = \"true\" ]; then command -v python3 >/dev/null || (apt-get update -qq && apt-get install -y -qq python3); fi"
          }
        ]
      }
    ]
  }
}
