# Debito Tecnico — RADAR IA

| ID | Debito | Contratto il | Costo di estinzione (ore) | Va estinto prima di |
|---|---|---|---|---|
| DT-0001 | `tecnico/ARCHITETTURA.md` e `repo-app/` scritti direttamente dal Regista, non da `architetto` + `sviluppatore-backend`/`sviluppatore-frontend` come da mandato (vedi `00_delibere/DEL-0003-regista-non-costruisce.md`) — scorciatoia presa per budget stretto, non autorizzata dal committente | 2026-08-30 | **Estinto (2026-08-30)**: `architetto` ha rivisto `ARCHITETTURA.md` (schema corretto, DT-0002); `sviluppatore-backend` ha rivisto `repo-app/` con occhi propri e applicato le correzioni DT-0002/DT-0003/DT-0004. Nessun residuo | — |
| DT-0002 | Schema dati `repo-app/schema.sql` conflate "risorsa" e "relazione utente-risorsa" in un'unica tabella; l'affermazione originaria che bastasse aggiungere `utente_id` "senza migrazione distruttiva" non regge (vedi `tecnico/ARCHITETTURA.md`, sezione "Schema dati"). Correzione proposta: separare `risorse` da `interazioni` ora, a costo marginale (nessun dato reale accumulato) | 2026-08-30 | **Estinto (2026-08-30)**: `schema.sql` ora ha due tabelle (`risorse`, `interazioni`); `server.py` aggiornato (JOIN in lettura, due INSERT/UPDATE in scrittura), stesso formato JSON verso il frontend, nessun dato da migrare (nessun dato reale ancora accumulato) | — |
| DT-0003 | `server.py`: validazione dello schema URL (`http`/`https`) presente solo lato client (`app.js`), assente lato server (`_crea_risorsa` valida solo la lunghezza) | 2026-08-30 | **Estinto (2026-08-30)**: aggiunta `url_valido()` in `server.py`, applicata in `_crea_risorsa`; url con schema diverso da http/https o non parsabile rifiutato con 400, coerente con gli altri messaggi di errore | — |
| DT-0004 | `server.py`: `connetti_db()` riapre connessione e riesegue `executescript(schema.sql)` a ogni richiesta HTTP; nessun test automatico presente sul percorso critico (mandato Sviluppatore Backend, `MANDATI.md` §5) | 2026-08-30 | **Estinto (2026-08-30)**: schema eseguito una sola volta in `inizializza_db()` (chiamata da `main()`); `connetti_db()` apre solo la connessione per richiesta. Aggiunto `repo-app/test_server.py` (7 test `unittest`, percorso critico dei 3 endpoint: lista, creazione con validazioni, patch stato, 404/400), lanciato con `python3 -m unittest test_server -v` (istruzione in `repo-app/README.md`) | — |
